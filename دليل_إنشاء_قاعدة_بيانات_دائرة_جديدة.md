# 🏛️ دليل إنشاء وربط قاعدة بيانات لأي دائرة جديدة (طبق الأصل)

تم تجهيز ملف النموذج الشامل لقاعدة البيانات: **[`database_master_template.sql`](file:///d:/الدوائر/الدائرة%20الثامنة%20عشر/18/database_master_template.sql)**.

هذا الملف يحتوي على كافة الجداول، العلاقات، المشغلات التلقائية (Triggers)، الحساب التلقائي لإجماليات الدفاتر، الإشعارات، وسياسات الأمان والحماية (RLS).

---

## 🚀 خطوات تجهيز قاعدة بيانات دائرة جديدة خلال دقيقتين:

### الخطوة 1: إنشاء مشروع جديد في Supabase
1. ادخل إلى موقع **[Supabase.com](https://supabase.com)** وسجّل الدخول.
2. اضغط على **New project** (مشروع جديد).
3. اختر اسماً للمشروع (مثلاً: `AlBunyan-District-19` أو اسم الدائرة الجديدة).
4. اكتب كلمة مرور قوية لقاعدة البيانات واحتفظ بها، ثم اختر المنطقة الأقرب (مثل `Frankfurt` أو أي سيرفر تختاره).
5. اضغط **Create new project** وانتظر دقيقة واحدة حتى يكتمل التجهيز.

---

### الخطوة 2: دمج النموذج في قاعدة البيانات (ضغطة زر واحدة)
1. من القائمة الجانبية في لوحة تحكم Supabase، اضغط على **SQL Editor** (أيقونة الأوامر البرمجية `>_`).
2. اضغط على زر **+ New query**.
3. افتح الملف: **[`database_master_template.sql`](file:///d:/الدوائر/الدائرة%20الثامنة%20عشر/18/database_master_template.sql)** وانسخ كامل محتواه.
4. الصق الكود داخل الـ **SQL Editor** في Supabase.
5. اضغط على زر **Run** (الأخضر في الأسفل).
> ✅ ستظهر لك رسالة: `Success. No rows returned`، وهذا يعني أن جميع الجداول، العلاقات، الإشعارات، والمشغلات أصبحت منشأة ومطابقة 100% لتطبيق الدائرة 18.

---

### الخطوة 3: إنشاء حساب المدير الأول (Admin)
1. من القائمة الجانبية، اذهب إلى **Authentication** ثم تبويب **Users**.
2. اضغط على **Add user** ثم **Create user**.
3. أدخل البريد الإلكتروني للمدير وكلمة المرور، وتأكد من تفعيل خيار **Auto Confirm User**.
4. اضغط **Create user**.
5. ارجع إلى **SQL Editor** وشغّل هذا السطر فقط لترقية الحساب إلى مدير عام:
```sql
UPDATE public.profiles 
SET role = 'admin' 
WHERE email = 'البريد_الذي_أنشأته@gmail.com';
```

---

### الخطوة 4: ربط كود تطبيق الدائرة الجديدة بقاعدة البيانات
في مجلد كود الدائرة الجديدة، افتح الملف:
`lib/supabase.js`
وقم بتعديل الرابط والمفتاح فقط بالقيم الخاصة بالمشروع الجديد (تجدها في Supabase في: `Project Settings` > `API`):

```javascript
const supabaseUrl = 'https://رابط_مشروعك_الجديد.supabase.co';
const supabaseAnonKey = 'المفتاح_الخاص_بالمشروع_الجديد_anon_key';
```

---

### 📦 ماذا يتضمن هذا النموذج تلقائياً؟
1. **جدول الحسابات (`profiles`)**: يدعم المدراء والحلفاء مع ربط مباشر بنظام مصادقة Supabase.
2. **جدول دفاتر الوصولات (`books`)**: يشمل أرقام الدفاتر، التسلسل، الشهور، وحساب إجمالي المبالغ وعدد الوصولات ذاتياً.
3. **جدول الوصولات (`receipts`)**: يحفظ بيانات المساهمين، المبالغ، والتواريخ مع منع تكرار أرقام الوصولات.
4. **جدول الإشعارات (`notifications`)**: ينبه المدراء تلقائياً فور قيام أي حليف بإضافة وصل جديد.
5. **المشغلات الآلية (Triggers)**: 
   - احتساب مجموع المبالغ فوراً بدون تدخل يدوي.
   - قفل وتحديث حالة الدفاتر.
   - ربط المستخدمين الجدد ببروفايلاتهم فور إنشائهم.
6. **سياسات الحماية (RLS)**: تضمن أن الحليف لا يرى إلا دفاتره ووصولاته الخاصة، بينما المدير يمتلك الصلاحية الشاملة للإدارة والتحكم.
